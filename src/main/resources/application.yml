spring:
  security:
    user:
      name: testing
      password: testing12313
  datasource:
    url: jdbc:postgresql://localhost:5423/STS
    username: test
    password: test
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect